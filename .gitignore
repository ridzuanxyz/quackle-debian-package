# Debian build artifacts
debian/.debhelper/
debian/*/
debian/debhelper-build-stamp
debian/files
debian/*.substvars
debian/*.debhelper.log

# Binary packages (should be in releases/ only)
*.deb
*.ddeb
*.changes
*.buildinfo

# Temporary files
*~
.#*
